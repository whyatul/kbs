---
const heroImages = [
    { 
        src: "https://images.unsplash.com/photo-1504307651254-35680f356dfd?w=800&q=85",
        alt: "Construction site",
        position: "top-[12%] left-[2%] md:top-[15%] md:left-[4%] lg:left-[5%] xl:left-[6%]",
        size: "w-32 h-40 sm:w-40 sm:h-52 md:w-52 md:h-64 lg:w-64 lg:h-80 xl:w-72 xl:h-[22rem]",
        delay: "0s",
        rotation: "-5deg"
    },
    { 
        src: "https://images.unsplash.com/photo-1519741497674-611481863552?w=800&q=85",
        alt: "Event celebration",
        position: "top-[6%] right-[2%] md:top-[8%] md:right-[4%] lg:right-[6%] xl:right-[8%]",
        size: "w-36 h-28 sm:w-44 sm:h-36 md:w-56 md:h-44 lg:w-72 lg:h-56 xl:w-80 xl:h-64",
        delay: "0.1s",
        rotation: "4deg"
    },
    { 
        src: "https://images.unsplash.com/photo-1618221195710-dd6b41faaea6?w=800&q=85",
        alt: "Modern interior",
        position: "top-[38%] left-[1%] md:top-[40%] md:left-[2%] lg:left-[3%] xl:left-[4%]",
        size: "w-28 h-36 sm:w-36 sm:h-48 md:w-44 md:h-60 lg:w-56 lg:h-72 xl:w-64 xl:h-80",
        delay: "0.2s",
        rotation: "3deg"
    },
    { 
        src: "https://images.unsplash.com/photo-1581092160562-40aa08e78837?w=800&q=85",
        alt: "Workers team",
        position: "top-[32%] right-[1%] md:top-[35%] md:right-[2%] lg:right-[3%] xl:right-[4%]",
        size: "w-32 h-44 sm:w-40 sm:h-56 md:w-48 md:h-68 lg:w-60 lg:h-80 xl:w-68 xl:h-[22rem]",
        delay: "0.15s",
        rotation: "-4deg"
    },
    { 
        src: "https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=800&q=85",
        alt: "Cleaning service",
        position: "bottom-[18%] left-[3%] md:bottom-[20%] md:left-[5%] lg:left-[6%] xl:left-[7%]",
        size: "w-28 h-32 sm:w-36 sm:h-44 md:w-44 md:h-56 lg:w-56 lg:h-68 xl:w-64 xl:h-76",
        delay: "0.25s",
        rotation: "5deg"
    },
    { 
        src: "https://images.unsplash.com/photo-1513623935135-c896b59073c1?w=800&q=85",
        alt: "Trade show",
        position: "bottom-[12%] right-[2%] md:bottom-[15%] md:right-[4%] lg:right-[5%] xl:right-[7%]",
        size: "w-36 h-28 sm:w-44 sm:h-36 md:w-56 md:h-44 lg:w-72 lg:h-56 xl:w-80 xl:h-64",
        delay: "0.3s",
        rotation: "-3deg"
    },
];

const services = [
    "Civil Construction",
    "Event Management", 
    "Interior Design",
    "Manpower Supply",
    "Housekeeping",
    "Custom Solutions"
];
---

<section
    class="min-h-screen flex flex-col items-center justify-center relative overflow-hidden px-4 sm:px-6 md:px-8 pt-20 sm:pt-24 md:pt-0"
    id="hero"
>
    <!-- Background -->
    <div id="hero-background" class="absolute inset-0 z-0">
        <!-- Gradient orbs - responsive sizes -->
        <div class="absolute top-1/4 left-1/4 w-[300px] sm:w-[400px] md:w-[500px] lg:w-[600px] h-[300px] sm:h-[400px] md:h-[500px] lg:h-[600px] bg-orange-500/10 rounded-full blur-[100px] sm:blur-[120px] md:blur-[150px] animate-pulse-slow"></div>
        <div class="absolute bottom-1/4 right-1/4 w-[250px] sm:w-[350px] md:w-[400px] lg:w-[500px] h-[250px] sm:h-[350px] md:h-[400px] lg:h-[500px] bg-purple-500/8 rounded-full blur-[80px] sm:blur-[100px] md:blur-[120px] animate-pulse-slow" style="animation-delay: 2s;"></div>
        
        <!-- Subtle grid -->
        <div class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.015)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.015)_1px,transparent_1px)] bg-[size:40px_40px] sm:bg-[size:60px_60px] md:bg-[size:80px_80px] [mask-image:radial-gradient(ellipse_60%_60%_at_50%_50%,black,transparent)]"></div>
        
        <!-- Noise texture -->
        <div class="absolute inset-0 opacity-[0.015] bg-[url('data:image/svg+xml;base64,')]"></div>
    </div>

    <!-- Floating Images - Visible on all screens -->
    <div class="absolute inset-0 z-[1] pointer-events-none">
        {heroImages.map((image, index) => (
            <div 
                class={`hero-image absolute ${image.position} ${image.size} rounded-xl md:rounded-2xl overflow-hidden shadow-xl md:shadow-2xl animate-float-in`}
                style={`animation-delay: ${image.delay}; transform: rotate(${image.rotation});`}
                data-parallax
                data-speed={(index % 3) + 1}
            >
                <img 
                    src={image.src} 
                    alt={image.alt}
                    class="w-full h-full object-cover"
                    loading="lazy"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent"></div>
                <div class="absolute inset-0 border border-white/20 rounded-xl md:rounded-2xl"></div>
            </div>
        ))}
    </div>

    <!-- Main Content -->
    <div class="relative z-10 text-center px-2 sm:px-4 md:px-6 lg:px-8 max-w-6xl mx-auto w-full">
        <!-- Top Label -->
        <div class="mb-4 sm:mb-6 md:mb-8 lg:mb-10 animate-fadeInUp opacity-0" style="animation-delay: 0.2s;">
            <span class="inline-flex items-center gap-1.5 sm:gap-2 px-3 sm:px-4 py-1.5 sm:py-2 bg-white/5 border border-white/10 rounded-full text-[10px] sm:text-xs md:text-sm text-zinc-400 backdrop-blur-sm">
                <span class="w-1.5 h-1.5 sm:w-2 sm:h-2 bg-orange-500 rounded-full animate-pulse"></span>
                Trusted by 50+ businesses
            </span>
        </div>

        <!-- Main Heading -->
        <h1 class="mb-4 sm:mb-6 md:mb-8 animate-fadeInUp opacity-0" style="animation-delay: 0.4s;">
            <span class="block text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl 2xl:text-8xl font-black tracking-tight leading-[0.95] text-white">
                ONE PARTNER
            </span>
            <span class="block text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl 2xl:text-8xl font-black tracking-tight leading-[0.95] mt-2 sm:mt-4 md:mt-6 lg:mt-8">
                <span class="font-peace text-gradient">Every Solution</span>
            </span>
        </h1>
        
        <!-- Service Tags -->
        <div class="flex flex-wrap justify-center gap-1.5 sm:gap-2 md:gap-3 mb-6 sm:mb-8 md:mb-10 lg:mb-12 animate-fadeInUp opacity-0 max-w-xl md:max-w-2xl lg:max-w-3xl mx-auto" style="animation-delay: 0.8s;">
            {services.map((service, index) => (
                <span 
                    class="service-tag px-2.5 sm:px-3 md:px-4 py-1 sm:py-1.5 md:py-2 bg-zinc-900/80 border border-zinc-800 rounded-full text-[10px] sm:text-xs md:text-sm text-zinc-300 cursor-default transition-all duration-300 hover:bg-orange-500/20 hover:border-orange-500/50 hover:text-orange-400"
                    style={`animation-delay: ${0.8 + index * 0.1}s;`}
                >
                    {service}
                </span>
            ))}
        </div>
    </div>
</section>

<style>
    @keyframes float-in {
        0% {
            opacity: 0;
            transform: translateY(30px) rotate(var(--rotation, 0deg)) scale(0.9);
        }
        100% {
            opacity: 1;
            transform: translateY(0) rotate(var(--rotation, 0deg)) scale(1);
        }
    }
    
    .animate-float-in {
        animation: float-in 1s ease-out forwards;
    }
    
    @keyframes pulse-slow {
        0%, 100% { opacity: 0.5; transform: scale(1); }
        50% { opacity: 0.8; transform: scale(1.05); }
    }
    
    .animate-pulse-slow {
        animation: pulse-slow 6s ease-in-out infinite;
    }
    
    .hero-image {
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        pointer-events: auto;
        will-change: transform;
        opacity: 0.7;
    }
    
    @media (min-width: 768px) {
        .hero-image {
            opacity: 0.85;
        }
    }
    
    @media (min-width: 1024px) {
        .hero-image {
            opacity: 1;
        }
    }
    
    .hero-image img {
        transition: transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
        filter: brightness(1);
    }
    
    .hero-image:hover {
        z-index: 50 !important;
        transform: rotate(0deg) scale(1.15) !important;
        box-shadow: 0 30px 60px rgba(0, 0, 0, 0.7), 0 0 40px rgba(249, 115, 22, 0.4);
        opacity: 1 !important;
    }
    
    .hero-image:hover img {
        transform: scale(1.1);
        filter: brightness(1.2);
    }
    
    .hero-image:hover .absolute.bg-gradient-to-t {
        opacity: 0.5;
    }
    
    .service-tag {
        backdrop-filter: blur(8px);
    }
    
    /* Make images more visible on mobile */
    @media (max-width: 640px) {
        .hero-image {
            filter: brightness(1.1);
        }
    }
</style>

<script>
    // Parallax effect for floating images
    const parallaxImages = document.querySelectorAll('[data-parallax]');
    
    if (parallaxImages.length > 0) {
        let ticking = false;
        
        const updateParallax = () => {
            const scrollY = window.scrollY;
            const windowHeight = window.innerHeight;
            
            parallaxImages.forEach((image) => {
                const imgEl = image as HTMLElement;
                const speed = parseFloat(imgEl.dataset.speed || '1');
                const rect = imgEl.getBoundingClientRect();
                
                // Only animate if in viewport
                if (rect.top < windowHeight && rect.bottom > 0) {
                    const yOffset = scrollY * speed * 0.08;
                    const rotation = imgEl.style.transform.match(/rotate\(([^)]+)\)/)?.[1] || '0deg';
                    imgEl.style.transform = `translateY(${yOffset}px) rotate(${rotation})`;
                }
            });
            
            ticking = false;
        };
        
        window.addEventListener('scroll', () => {
            if (!ticking) {
                requestAnimationFrame(updateParallax);
                ticking = true;
            }
        }, { passive: true });
    }
    
    // Mouse move effect for images (desktop only)
    if (window.innerWidth >= 1024) {
        const heroSection = document.getElementById('hero');
        const floatingImages = document.querySelectorAll('.hero-image');
        
        if (heroSection && floatingImages.length > 0) {
            let rafId: number | null = null;
            
            heroSection.addEventListener('mousemove', (e) => {
                if (rafId) return;
                
                rafId = requestAnimationFrame(() => {
                    const { clientX, clientY } = e;
                    const centerX = window.innerWidth / 2;
                    const centerY = window.innerHeight / 2;
                    
                    const moveX = (clientX - centerX) / centerX;
                    const moveY = (clientY - centerY) / centerY;
                    
                    floatingImages.forEach((image, index) => {
                        const imgEl = image as HTMLElement;
                        const speed = ((index % 3) + 1) * 6;
                        const x = moveX * speed;
                        const y = moveY * speed;
                        const rotation = imgEl.style.transform.match(/rotate\(([^)]+)\)/)?.[1] || '0deg';
                        
                        if (!imgEl.matches(':hover')) {
                            imgEl.style.transform = `translate(${x}px, ${y}px) rotate(${rotation})`;
                        }
                    });
                    
                    rafId = null;
                });
            });
            
            heroSection.addEventListener('mouseleave', () => {
                floatingImages.forEach((image) => {
                    const imgEl = image as HTMLElement;
                    const rotation = imgEl.style.transform.match(/rotate\(([^)]+)\)/)?.[1] || '0deg';
                    imgEl.style.transform = `rotate(${rotation})`;
                });
            });
        }
    }
</script>
